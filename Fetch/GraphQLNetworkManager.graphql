mutation Login($auth: AuthInput!) {
    createUser(auth: $auth, user:{
          zipcode: "99999"
    }) {
        token
    }
}

query UserIsOnboarded {
    currentUser {
        pet_size_preference
    }
}
