mutation Login($auth: AuthInput!) {
    createUser(auth: $auth, user:{
          zipcode: "99999"
    }) {
        token
    }
}

query IsUserOnboarded {
    currentUser {
        pet_size_preference
    }
}
